# -*- coding: utf-8 -*-
"""lignin.ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1sCMb4QIDie82RpmWLPAJlCsWDms7Qci8
"""

# Define the original masses and total mass
original_white_cement = 29.7  # g
original_sand = 59  # g
original_dolomite = 10  # g
original_mhec = 0.3  # g
original_lignin = 1  # g
total_mass = 100  # g

# Reduce lignin to 0.3 g
new_lignin = 0.5 # g

# Calculate the increase factor for other components
increase_factor = (total_mass - new_lignin) / (total_mass - original_lignin)

# Calculate the new masses
new_white_cement = original_white_cement * increase_factor
new_sand = original_sand * increase_factor
new_dolomite = original_dolomite * increase_factor
new_mhec = original_mhec * increase_factor

# Print the results
print("New Masses:")
print(f"White Cement: {new_white_cement} g")
print(f"Sand: {new_sand} g")
print(f"Dolomite: {new_dolomite} g")
print(f"MHEC: {new_mhec} g")
print(f"Lignin: {new_lignin} g")

# Step 1: Mount Google Drive (if not already mounted)
from google.colab import drive
drive.mount('/content/drive')

# Step 2: Clone the GitHub repository to Colab
import os

# Replace 'your_username' and 'your_repo' with your GitHub username and repository name
github_username = 'your_username'
github_repo = 'your_repo'
repo_url = f'https://github.com/{github_username}/{github_repo}.git'

# Clone the repository
if not os.path.exists(github_repo):
    !git clone $repo_url

# Change to the repository directory
os.chdir(github_repo)

# Step 3: Save your code changes and push to GitHub
# Add the changes
!git add .

# Commit the changes
commit_message = 'Update from Colab'
!git commit -m "$commit_message"

# Push the changes to GitHub (you might need to provide your GitHub username and password/token)
!git push origin master  # Use 'main' instead of 'master' if your branch is named 'main'